// 이 서비스 워커는 오프라인 지원이나 캐싱 전략을 추가하지 않는다. 브라우저(특히 Chrome)가
// "홈 화면에 추가" 설치 프롬프트(beforeinstallprompt)를 띄우려면 fetch 이벤트를 가진
// 서비스 워커가 등록되어 있어야 하므로, 요청을 그대로 네트워크로 통과시키기만 하는
// 최소한의 핸들러만 둔다. 앱의 데이터 처리/네트워크 동작은 전혀 바뀌지 않는다.
self.addEventListener("fetch", () => {
  // 응답을 가로채거나 캐시하지 않는다 — 아무 것도 하지 않으면 브라우저가 평소처럼 처리한다.
});
